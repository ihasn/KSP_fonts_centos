This worked on CentOS 6.4 x86_64.  It is a noarch and should work on x86 (i686) as well.
It has not been tested to work on RedHat or Fedora builds but should work on them.
Read the INSTALL for how to install.  You will need sudo access.
